import React from "react";

const MonthFilter = ({ data }) => {
  console.log({ data });
  //   const
  return (
    <div>
      {/* {data.map((item, index) => {
        return <p key={index}>{item}</p>;
      })} */}
    </div>
  );
};

export default MonthFilter;
